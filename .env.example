# Content Marketing Pipeline Environment Configuration
# Copy this file to .env and add your actual values

# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Override default OpenAI model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o

# Optional: Adjust request timeout in seconds (default: 60)
# OPENAI_TIMEOUT=60

# Optional: Set custom OpenAI organization ID
# OPENAI_ORG_ID=your-org-id-here

# Optional: Custom base URL for OpenAI API (for custom deployments)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Application Configuration
# Optional: Override default timezone for scheduling
# DEFAULT_TIMEZONE=UTC

# Optional: Set default target audience
# DEFAULT_AUDIENCE=B2B professionals

# Optional: Set default brand voice
# DEFAULT_BRAND_VOICE=professional

# Optional: Set default word count for blogs
# DEFAULT_WORD_COUNT=500

# Optional: Set output directory path
# OUTPUT_DIRECTORY=output

# Debug and Development Settings
# Optional: Enable verbose logging (true/false)
# VERBOSE_LOGGING=true

# Optional: Save raw API responses for debugging (true/false)
# SAVE_RAW_RESPONSES=false

# Optional: Enable timing information (true/false)
# TIMING_ENABLED=true

# Optional: Set maximum retries for failed requests
# MAX_RETRIES=3

# Optional: Set retry delay in seconds
# RETRY_DELAY=5

# Performance Settings
# Optional: Enable request rate limiting (requests per minute)
# RATE_LIMIT=10

# Optional: Enable caching of API responses (true/false)
# CACHE_ENABLED=false

# Advanced Configuration
# Optional: Custom configuration file path
# CONFIG_FILE=config/config.yaml

# Optional: Enable custom workflow support (true/false)
# CUSTOM_WORKFLOWS_ENABLED=true

# Optional: Set maximum number of topics to research
# MAX_TOPICS=5

# Optional: Set minimum confidence score for topic selection (0-10)
# MIN_TOPIC_CONFIDENCE=6

# Notes:
# 1. OPENAI_API_KEY is the only required environment variable
# 2. All other variables are optional and have sensible defaults
# 3. Boolean values should be 'true' or 'false' (lowercase)
# 4. Remove the # at the beginning of lines to activate optional settings
# 5. Never commit your actual .env file to version control
